# Aegis — Greek Myth Chess (Beast Mode)

3D bronze/marble Greek-myth chess with react-three-fiber + drei, chess.js bot (Stockfish autodetect + minimax fallback), Learn, Puzzles, PGN.

## Run
npm install
npm run dev

## Build
npm run build

Deploy `dist/` to Netlify or Vercel.
